name: My First Workflows
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: |
                echo " Hello world from Github Actions"

            - name: List Files ${{ github.repository }}
              run: ls ${{ github.repository }}

            - name: Read file
              run: cat README.md